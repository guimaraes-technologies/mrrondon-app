<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pages="clr-namespace:MrRondon.Pages;assembly=MrRondon"
             xmlns:forms="clr-namespace:FFImageLoading.Forms;assembly=FFImageLoading.Forms"
             xmlns:transformations="clr-namespace:FFImageLoading.Transformations;assembly=FFImageLoading.Transformations"
             x:Class="MrRondon.Pages.Company.CompanyDetailsPage"
             Title="{Binding Title}"
             BackgroundColor="{StaticResource PageBackgroundColor}">

    <ScrollView>
        <StackLayout Margin="0" Padding="0">
            <Image Source="{Binding Company.ImageSourceCover}" HeightRequest="200" Aspect="Fill"></Image>
            <StackLayout Padding="{StaticResource PagePadding}">
                <Label Text="{Binding Company.Name}" TextColor="Black" FontSize="Large" HorizontalOptions="CenterAndExpand"></Label>

                <StackLayout Orientation="Horizontal">
                    <Label Text="LIGAR" TextColor="{StaticResource PrimaryColor}" HorizontalOptions="Start">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding MakePhoneCallCommand}"></TapGestureRecognizer>
                        </Label.GestureRecognizers>
                    </Label>
                    <Label Text="VER LOCAL NO MAPA" TextColor="{StaticResource PrimaryColor}" HorizontalOptions="EndAndExpand">
                        <Label.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding OpenMapCommand}"></TapGestureRecognizer>
                        </Label.GestureRecognizers>
                    </Label>
                </StackLayout>

                <Label Text="Nome Fantasia" Style="{StaticResource PrimaryLabel}" />
                <Label Text="{Binding Company.FancyName}" Style="{StaticResource SecondaryLabel}" />

                <Label Text="Segmento" Style="{StaticResource PrimaryLabel}" />
                <Label Text="{Binding Company.SegmentDescription}" Style="{StaticResource SecondaryLabel}" />

                <Label Text="CNPJ" Style="{StaticResource PrimaryLabel}" />
                <Label Text="{Binding Company.Cnpj}" Style="{StaticResource SecondaryLabel}" />

                <ListView ItemsSource="{Binding Company.Contacts}"
                      SeparatorVisibility="None"
                      HasUnevenRows="False"
                      HeightRequest="40">
                    <ListView.Header>
                        <Label Text="Contatos:" Style="{StaticResource PrimaryLabel}" />
                    </ListView.Header>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Label Text="{Binding ContactDetail}" Style="{StaticResource SecondaryLabel}"></Label>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.Footer>
                        <Label Text="Nenhum informado" IsVisible="{Binding NotHasContacts}" TextColor="{StaticResource SecondaryLabelColor}"></Label>
                    </ListView.Footer>
                </ListView>

                <Label Text="Endereço" Style="{StaticResource PrimaryLabel}" />
                <Label Text="{Binding Company.Address.FullAddress}" Style="{StaticResource SecondaryLabel}" />

                <pages:FooterPage />
            </StackLayout>
        </StackLayout>
    </ScrollView>
</ContentPage>